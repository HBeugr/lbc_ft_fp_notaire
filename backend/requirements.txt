# Web framework
# ─────────────────────────────────────────────────────────────────────────────
# Audit de sécurité des dépendances (pip-audit). Versions relevées :
#   python-jose      3.3.0 → 3.4.0    (PYSEC-2024-232/233, 2025-185 : confusion
#                                       d'algorithme — non exploitable ici car le
#                                       décodage impose HS256 — et DoS JWE)
#   python-multipart 0.0.12 → 0.0.32  (traversée de chemin + plusieurs DoS de
#                                       parsing multipart : parseur exposé en frontal)
#   cryptography     43.0.1 → 44.0.1  (PYSEC-2026-1284)
#   lxml             5.3.0 → 5.4.0    (correctif de branche 5.x)
#
# CVE résiduelles ASSUMÉES, car le correctif impose un saut majeur incompatible
# avec FastAPI 0.115 (starlette < 0.39) et sans exploitabilité démontrée sur
# notre surface :
#   starlette 0.38.6  — validation du header Host (PYSEC-2026-161). Le tenant est
#                       résolu depuis le JWT, jamais depuis Host : pas de décision
#                       de sécurité fondée sur cet en-tête. À traiter lors d'une
#                       montée coordonnée FastAPI + Starlette.
#   cryptography      — correctifs suivants en 46.x ; le nôtre couvre la CVE
#                       exploitable, les autres visent des primitives non utilisées.
#   lxml 6.x, pdfminer, ecdsa — dépendances transitives de parsing PDF/HTML des
#                       listes de sanctions ; entrées maîtrisées (fichiers officiels
#                       chargés par l'admin), pas de flux externe arbitraire.
# ─────────────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
asyncpg==0.30.0
# Requis par le mode asynchrone de SQLAlchemy (pont greenlet/asyncio).
greenlet==3.1.1
alembic==1.13.3

# Auth & Security
python-jose[cryptography]==3.4.0
bcrypt==4.2.1
pyotp==2.9.0
redis==5.1.1

# File storage
minio==7.2.9

# Encryption
cryptography==44.0.1

# PDF generation
fpdf2==2.8.3

# Traitement d'image — validation des logos de cabinet (dimensions, format réel).
# Tirée transitivement par fpdf2, mais épinglée car nous en dépendons directement.
pillow==12.3.0

# Sanctions list parsing
pdfplumber==0.11.4
beautifulsoup4==4.12.3
lxml==5.4.0

# Fuzzy matching (sanctions)
rapidfuzz==3.10.0

# Date arithmetic
python-dateutil==2.9.0

# Excel export
openpyxl==3.1.5

# Serialization
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Structured logging
structlog==24.4.0

# HTTP client
httpx==0.27.2

# Utils
python-multipart==0.0.32
python-dotenv==1.0.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
anyio==4.6.2
aiosqlite==0.20.0
