[pytest]
asyncio_mode = auto
# Les cabinets de test sont provisionnés une fois pour la session (création de
# schéma + migrations = plusieurs secondes) : fixtures et tests doivent donc
# partager la même boucle d'événements.
asyncio_default_fixture_loop_scope = session
testpaths = tests
